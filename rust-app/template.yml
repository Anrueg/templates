$schema: "https://moonrepo.dev/schemas/template.json"

extends: ["configs"]
id: rust-app
title: Rust application
description: Add rust application
destination: rust/[name]
variables:
  name:
    type: "string"
    default: "[project_slug]"
    required: true
    prompt: "Application name?"
