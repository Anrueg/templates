$schema: "https://moonrepo.dev/schemas/project.json"

id: "rust/{{ name }}"

project:
  name: "{{ name }}"
  description: "Rust application: {{ name }}"

type: application
stack: backend
language: rust
tags: [rust, rust-app]

# TODO
# fileGroups:

# toolchain:
#   default: rust
