$schema: "https://moonrepo.dev/schemas/project.json"

id: "rust/{{ name }}"

project:
  name: "{{ name }}"
  description: "Rust library: {{ name }}"

type: library
stack: backend
language: rust
tags: [rust, rust-lib]

# TODO
# fileGroups:

# toolchain:
#   default: rust
